# eslint-config-next@16.3.1 pide eslint >=9 como peer dep, pero el proyecto
# fija eslint@8.57.0 (ver CLAUDE.md, sección de deploy) — sin esto, tanto
# `npm install` en limpio como el build de Vercel fallan con ERESOLVE.
legacy-peer-deps=true
